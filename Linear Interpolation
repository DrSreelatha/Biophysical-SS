x = [1, 2, 4, 5];
y = [1, 4, 16, 25];
xi = 1:0.1:5;
yi = interp1(x, y, xi, 'linear');
plot(x, y, 'o', xi, yi, '-');
xlabel('x');
ylabel('y');
title('Linear Interpolation');
legend('Data points',
'Interpolated points');
